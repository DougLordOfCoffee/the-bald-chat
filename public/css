.side {
  display: flex;
  flex-direction: column;
  gap: 28px; /* Adjust this value for more or less spacing */
}
.small {
  font-size: 0.85rem;
  color: var(--muted);
}
.logo {
  width: 46px;
  height: 46px;
  border-radius: 10px;
  overflow: hidden;
}

.logo img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
